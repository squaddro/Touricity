<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="DollarRatingBar" parent="Widget.AppCompat.RatingBar.Small">
        <item name="android:progressDrawable">@drawable/dollar_rating_bar</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:maxHeight">48dp</item>
    </style>
    <style name="DollarRatingBarFilter" parent="Widget.AppCompat.RatingBar.Small">
        <item name="android:progressDrawable">@drawable/dollar_rating_bar_filter</item>
        <item name="android:minHeight">10dp</item>
        <item name="android:maxHeight">64dp</item>
    </style>
</resources>
